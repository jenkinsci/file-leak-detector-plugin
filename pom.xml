<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.jenkins-ci.plugins</groupId>
    <artifactId>plugin</artifactId>
    <version>1.424</version><!-- which version of Jenkins is this plugin built against? -->
  </parent>

  <groupId>com.cloudbees.jenkins.plugins</groupId>
  <artifactId>cloudbees-file-leak-detector</artifactId>
  <name>CloudBees File Leak Detector Plugin</name>
  <description>Monitors and detects file handle leaks in Jenkins</description>
  <version>1.1-SNAPSHOT</version>
  <packaging>hpi</packaging>

  <dependencies>
    <dependency>
      <groupId>org.kohsuke</groupId>
      <artifactId>file-leak-detector</artifactId>
      <version>1.4</version>
      <classifier>jar-with-dependencies</classifier>
    </dependency>
  </dependencies>

  <scm>
    <connection>scm:git:ssh://git@git.cloudbees.com/cloudbees/${project.artifactId}-plugin.git</connection>
    <developerConnection>scm:git:ssh://git@git.cloudbees.com/cloudbees/${project.artifactId}-plugin.git</developerConnection>
  </scm>

  <repositories>
    <repository>
      <id>repository-cloudbees.forge.cloudbees.com</id>
      <url>https://repository-cloudbees.forge.cloudbees.com/private-release/</url>
    </repository>
    <repository>
      <id>repo.jenkins-ci.org</id>
      <url>http://repo.jenkins-ci.org/public/</url>
    </repository>
  </repositories>

  <distributionManagement>
    <repository>
      <id>repository-cloudbees.forge.cloudbees.com</id>
      <url>dav:https://repository-cloudbees.forge.cloudbees.com/private-release/</url>
    </repository>
    <snapshotRepository>
      <id>repository-cloudbees.forge.cloudbees.com</id>
      <url>dav:https://repository-cloudbees.forge.cloudbees.com/private-snapshot/</url>
    </snapshotRepository>
  </distributionManagement>
</project>
